# VideoKit 接手维护说明

## 当前状态（2026-09-15）

- 来源：当前目录的 VideoKit-main.zip，原始版本 4.4.39。
- 压缩包 SHA256：9BEB8E2DAD05DEAE3A40EE1563367E3389806D7004E44B179BC7EB47C3E282FC。
- 已解压到 VideoKit-main，保留原压缩包；未改动业务源码及依赖声明。
- 已使用原 package-lock.json 执行 npm ci，安装 494 个包。本机 Node.js 为 v24.13.0。
- 新增 start-videokit.cmd，以及上一级的“启动VideoKit.cmd”。

## 启动与日常开发

双击上一级“启动VideoKit.cmd”，或本目录 start-videokit.cmd。
首次缺少依赖时自动运行 npm ci；需要网络。使用 Node.js 和 npm。
启动器同时运行 Vite 与 Electron；关闭软件后结束配套开发服务。启动失败会保留终端错误。
默认端口 5173；占用时应关闭已有开发服务，或在 CMD 中先设置 VITE_PORT 再启动。
依赖更新后，在本目录手动运行 npm ci 以同步锁文件。

本启动器直接调用原有工具，绕开 package.json 中 ${VITE_PORT:-5173} 这一不兼容 Windows CMD 的写法。
未改变原 npm start，因此 Windows 下请使用新增 CMD。

媒体工具通过 FFMPEG_PATH、FFPROBE_PATH 指向上一级现有 exe；已设置的环境变量优先。
移动整个项目时，要同时提供 FFmpeg/FFprobe，或设置这两个环境变量，或使用 vendor/windows/ffmpeg/bin 布局。
源码开发模式修改 src 可由 Vite 更新；修改 Electron 主进程代码需关闭并重新启动。

## 源码地图

| 位置 | 维护用途 |
| --- | --- |
| src/index.html、styles.css | 页面结构、样式 |
| src/app.js | 前端主交互 |
| src/batch-reels.js、reels-batch-table.js | 批量短视频、任务表格 |
| src/reels-timeline*.js、reels-preview-v2.js | 时间线和预览 |
| src/reels-*-export.js、reels-export-engine.js | 导出相关逻辑 |
| electron/main.js | 桌面窗口、生命周期、媒体环境和 IPC |
| electron/preload.js | 前端与 Electron 的桥接 |
| electron/apiRouter.js | 本地 API 路由 |
| electron/services/ffmpeg.js | 媒体处理命令 |
| electron/services/settings.js | 配置保存和目录管理 |
| electron/services/cloudTranscription.js、gladia.js、gemini.js、elevenlabs.js | 转录和 AI 服务 |
| electron/updater.js | 自动更新、测试版通道和绿色版更新 |
| backend/ | 历史 Python 后端及辅助代码；当前主程序已使用 Node.js API |
| vendor/ | 外部运行工具、模型等；压缩包未包含完整 Windows 运行工具 |
| package.json、package-lock.json | 版本、依赖、打包参数 |
| vite.config.js | 前端构建、静态 JS 复制和端口 |
| .github/workflows/build.yml | 多平台自动构建和发布 |
| .agents/AGENTS.md | 项目版本与发布约定 |

## 验证结果和边界

- start-videokit.cmd --check：通过，依赖及 Electron 可执行文件存在。
- npm run build：通过，包括 prebuild；原检查有一项 davinciFusionExport.js Unix 命令警告。
- Vite 对非 module 脚本发出警告；项目已有复制插件，将 33 个 JS 文件复制到 dist。
- 实际 CMD 启动通过：Electron 窗口标题“VideoKit - 视频工具包”，进程响应正常，API 注册成功、字体扫描和批量界面初始化完成。
- 日志有 Chromium 缓存拒绝访问警告，未阻止本次窗口启动；若遇到缓存问题，结合系统目录权限与应用缓存设置排查。
- 日志会提示 vendor 中没有 FFmpeg；启动器使用外部环境路径，媒体服务命令已单独验证。
- 未完整回归转录、付费 AI、导出、下载和口型同步；这些功能需要各自的密钥、工具或模型。
- package.json 声明 npm test，但压缩包没有 tests 目录，.gitignore 还忽略了 tests/。接手时应补齐可版本管理的回归测试。

## 后续更新维护

1. 先建立你控制的独立 Git 仓库或 fork，保存此版本作为基线。ZIP 不包含原 Git 历史；当前目录还在 Subtitle-Composer 的 Git 工作区内部，提交时注意项目边界。
2. 维护代码时按“前端 → preload → apiRouter → services”的调用方向定位问题。
3. 发布前调整 package.json 的 build.publish，目前为 secure-artifacts/VideoKit；同时检查 electron/updater.js 中同名默认值以及 Actions 发布设置。
4. 决定是否继续使用原 appId com.timcode.videokit 和产品名；这关系到安装与用户数据隔离。
5. 按 .agents/AGENTS.md 同步 package.json、package-lock.json 的版本。先提交推送主分支，再推版本 tag；保留各平台 latest*.yml 更新元数据。
6. 构建验证使用 npm run build。Windows 打包可使用 npm run electron:build -- --win --x64；正式打包前补全 vendor 资源，当前验证仅覆盖前端构建和源码启动。
7. 开发日志在 logs/。配置通常在 Electron userData/backend；默认 Windows 位置为用户 AppData/Roaming/videokit，应用支持更改缓存位置，应以应用实际设置为准。备份用户数据后再做迁移。
8. 当前快照根目录没有看到项目级 LICENSE；正式对外分发前需核实原项目授权，不能把字体许可当作项目许可。

本次没有配置你的远程仓库，也没有发布版本或切换更新源。以上为本地快照的检查结果。
## 本地新增功能：Groq 入口与队列库（2026-09-15）

任务表顶部新增：
- **转录 API · Groq / Gladia**：选择 Groq，填入 API Key，点击“保存并设为首选”。复用原有 Groq Whisper Large V3 Turbo 适配器和逐词时间戳。旧的各服务商密钥配置仍保留。
- **保存队列**：命名保存全部标签、任务、字幕文本与时间轴、样式、素材路径及导出参数。同名保存生成独立快照。
- **调用队列**：窗口按时间列出名称、保存时间、标签/任务数、去重后的音频/视频文件数、字幕任务数。调用前自动备份当前非空队列。

队列保存在 Electron userData/queue-library 下，重启可继续调用。已有 SRT 会连同内容保存；调用时写入独立字幕副本，避免修改原字幕。音视频保存的是本地文件关联，不复制大文件，原素材不要移动或删除。

新增代码：electron/services/queueLibrary.js、src/reels-queue-library.js；入口在 electron/apiRouter.js、src/reels-batch-table.js、src/index.html。

验证：node --test scripts/check-queue-features.cjs（4 项）；npm run build；隔离 Electron 实测保存、列表、调用、自动备份与 Groq 设置通过。真实云端转录没有使用用户密钥调用。
UI 测试使用 scripts/check-queue-ui.cjs 和 scripts/queue-ui-test-entry.cjs，需要本地 Vite 服务及 PLAYWRIGHT_MODULE 环境变量指向已安装 Playwright。测试将用户数据写到系统临时目录，转录设置使用内存替身。
Groq 接口核对来源：https://console.groq.com/docs/speech-to-text

## 微软配音（2026-09-20）
任务表的人声工具已增加‘微软 Edge（无需 Key）’。本机已配置独立环境，支持声音选择、单条和批量配音、句子级字幕。使用及维护见 docs/微软Edge配音.md。已通过两行真实在线配音与队列保存的隔离桌面测试。

